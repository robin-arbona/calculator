import { DefaultTheme } from 'styled-components';

const defaultTheme : DefaultTheme = {
    border: '1px solid red',
    paddingButton: '10px',
    colors: {
        primary: 'red',
        secondary: '#fff',
    },
    size: '10px',
};

export { defaultTheme  };